architecture PythonArchitecture {
    component-and-connector-view::
        elements{
            component frontend_ms();
            component backend_ms(archivo);
            connector http HTTP;
        }
        relations{
            attachment (HTTP:course_ms, professor_ms);
        }
}

-- data model view
-- TSL 

-> API -> SourceCode 